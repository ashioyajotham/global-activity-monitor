{
  "name": "global-activity-monitor",
  "version": "4.1.0",
  "description": "Autonomous world activity tracker — GDELT theme-classified events, Natural Earth map, SQLite persistence",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "cleanup": "node -e \"require('./db').init(); require('./db').cleanup(30); require('./db').close();\""
  },
  "dependencies": {
    "better-sqlite3": "^11.7.0",
    "cors": "^2.8.5",
    "express": "^4.21.0",
    "node-cron": "^3.0.3",
    "rss-parser": "^3.13.0",
    "sentiment": "^5.0.2",
    "ws": "^8.18.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
